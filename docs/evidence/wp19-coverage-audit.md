# WP-19 coverage audit

Audit of the working tree at `/Users/kylegothman/dev/kt-wp19`, 16 September 2026. This maps the 47 acceptance criteria and V1 to V23 to source behavior and assertions actually present in the tests. All six separately proposed integrations were explicitly approved and applied, and their regression tests are present. **The full integration tree passed all four exact gates: 127 files and 2,156 tests passed, with no skips, in 78.49 seconds.** All four independently verified commits are pushed; the tip and gate ledger are in the final report. Test titles identify cases even when line numbers shift.

The accepted pre-flight rulings supersede conflicting original criteria: R8 corrects travel rounding and separates the analytical economy curve; R9 adds mid-leg credit; R10 explicitly defers generated reclamation balance; R11 fixes return rounding; R6 collects events through `onAny`; V4 calls the existing frame-policy setter. The six later approvals are applied changes, not remaining approval requests.

Status: **PASS** means the cited implementation and assertions satisfy the approved scope, supported by the confirmed full integration gate run. An amended or deferred part is stated explicitly; in particular, R10 does not establish the original generated-play balance claim. Optional coverage improvements are not failures or new approval requests. All four commits and the branch push are complete.

All test paths below are relative to the repository. Host/director coverage is consolidated in the owned `tests/game/runHost.test.ts`; the quantum regression is in `tests/game/legRunner.test.ts`. Temporary extra suite names are not used as final evidence.

## Acceptance criteria

| # | Coverage | Specific evidence or remaining gap |
|---|---|---|
| 1 | PASS | Full integration `npm run typecheck` exited 0 after all six approved changes and regressions. |
| 2 | PASS | Full integration `npm run test` exited 0: 127 files, 2,156 tests all passed, no skips, 78.49s; Node22 with temporary `VITEST_MAX_WORKERS=2`. No repository config change. The four historical protected failures are resolved by the approved edits. |
| 3 | PASS | Full integration `npm run build` exited 0, including TypeScript and production Vite. |
| 4 | PASS | `tests/game/travel/paceRations.test.ts`: `reproduces every numerical pace column and derives the overhead` asserts the complete four-by-six table and all four overheads. |
| 5 | PASS, amended R8 | `tests/game/travel/travelLoop.test.ts`: `uses the R8 runtime numbers for %s`; `tests/game/runHost.test.ts`: `integrates real kernel travel at %s with the R8 totals`. Both use ticks 134/80/54/39 and quota 335/200/135/97.5; original cycle and event-draw rows remain exact. |
| 6 | PASS | `paceRations.test.ts`: `uses fractional game reservations and a separate kernel allocation table`, including 4/2.5/1.5/.8 at five alive and linear examples at zero/three alive. |
| 7 | PASS | `tests/game/economyCurve.test.ts`: `scales and floors all twelve starting ledgers`, including compiler/architect 560/336/72/24. |
| 8 | PASS | `economyCurve.test.ts`: `matches all base dividends and clamps the throughput factor`, asserts all thirteen entries and 1326 sum. |
| 9 | PASS, amended R8 | `economyCurve.test.ts`: `reproduces narrative5.5 as an analytical fixture with rounded leg costs and half-away display`. Asserts all supplied rows, including 373, -9 and -28; explicitly excludes credit/deaths/reclamation and contrasts 2553 rounded versus 2550 exact cost. |
| 10 | PASS | `travelLoop.test.ts`: `contains 1020 travel segments, no Boot travel, and the seven authorised depots`; Boot has zero ticks/charges/draws. |
| 11 | PASS | Same case asserts the exact seven-element depot list and excludes `drowned_reach`. |
| 12 | PASS, amended R9 | `runHost.test.ts`: first unaffordable mandatory tick, conservative force, zero cycle cost, .3 tie-broken drain and refusal to raise pace on credit. `travelLoop.test.ts`: credit draw cadence and zero-cycle travel. `economyCurve.test.ts`: equality affordability boundary. `legRunner.test.ts`: `floors the outcome before the dividend and preserves ordered deduplication, credit=%s` asserts the exact .75 dividend factor. |
| 13 | PASS | `runHost.test.ts`: `forces starved rations at zero quota and refuses an attempted increase before mutation`, then permits increase after quota replenishment. `tests/game/reclamation/scoring.test.ts`: free first round at zero bandwidth. |
| 14 | PASS | `travelLoop.test.ts`: `keeps blocks and bandwidth nonnegative through 100,000 seeded economy ticks` now asserts floors, positive gains, exhaustion branches and periodic refills, in addition to fixed examples. |
| 15 | PASS | `tests/game/afflictions.test.ts`: all thirteen source rows, full remedy payloads, termination mapping, no `5.3.4`, and the `cache_thrash` corrected citation. |
| 16 | PASS | `afflictions.test.ts`: additive drain, each of six fatal clocks at the boundary, no reacquire/reset, repair-without-cure and tier-scaled clocks; `all nine remedy variants` now checks each matching remedy plus same-kind/cross-kind near misses and numeric surplus. `runHost.test.ts` covers carried-age rebasing and fatal-before-drain ordering. |
| 17 | PASS | `tests/game/convoy.test.ts`: `keeps integrity and status consistent across 10,000 seeded operations` now mixes drain, repair, acquire, cure and derezz/replacement, verifies clamps and status every step, observes every band, and probes random values across the interval. |
| 18 | PASS, approved pending-process exception | `runHost.test.ts`: `terminates a still-live bound pid and emits exactly one epitaph across repeated postTick input` checks one lifecycle exit, one tombstone and retained member. `discards a pending Vesper at tick 5 while retaining the pathology in the epitaph` asserts pending PCB termination with `killed_by_parent`, pathology `storage_corruption` in the epitaph, only state-change/syscall events, and no later admission or duplicate death. `keeps the chosen pathology on the lifecycle exit after Vesper has arrived` asserts the unchanged admitted-process reason/event path. Normal exits separately remain alive. |
| 19 | PASS | `tests/game/events/eventDeck.test.ts`: `10,000 draws are identical across equal seeds and across RNG save/restore`, including final RNG equality. |
| 20 | PASS | `eventDeck.test.ts`: throwing predicate excluded and diagnostic recorded through injected sandbox, remaining weights unchanged; separate eligibility/order/renormalisation test. |
| 21 | PASS | `eventDeck.test.ts`: positive integer weight, duplicate id, total weight, nonempty narration, plus valid table. |
| 22 | PASS, amended V8 | `tests/game/crossing/contention.test.ts`: six exact hand-built views, clamp/empty case, and matching current-wait ages using live quantum. The accepted wait-age proxy replaces unavailable hold history. |
| 23 | PASS | `tests/game/crossing/options.test.ts`: four `all narrative 12.3 cells at C=%s` cases assert costs, ticks and probabilities for every option, including displayed and exact wait probability. |
| 24 | PASS | `options.test.ts`: four unordered-block probability rows plus resolution failure inflicting `starvation`. |
| 25 | PASS | `options.test.ts`: retry case begins exactly at .70 and moves to 1.0, asserting both `lock_convoy` and `livelock`. An isolated case for each side would make failure attribution clearer but is not necessary for presence of this behavior. |
| 26 | PASS | `options.test.ts`: separate roll after both success and failure first rolls, plus the exact .12 boundary; checks death and fatal record. |
| 27 | PASS | `options.test.ts`: .97 monitor chance across 101 contention points; monitor failure costs 20 blocks and inflicts `bit_rot` before successful retry. |
| 28 | PASS | `options.test.ts` proves 136 real wait ticks, 13 draws and 340 prepaid quota at C=.8. `runHost.test.ts`: `expires %s at its carried deadline during a prepaid wait` covers all six non-null fatal clocks, exact death tick and lifecycle reason, while travel ticks remain zero; separate wait draw and prepaid affliction cases cover the shared director path. |
| 29 | PASS, limited strength | `options.test.ts`: 1000 `quoteAll` plus `quote` calls preserve an independently held crossing RNG state. Quote functions accept no RNG at all; the API and boundary scanner supplement this assertion. |
| 30 | PASS | `options.test.ts`: recomputed contention, four-attempt cap, exact summed cycle/tick costs and actual kernel advancement; separate wait/monitor real-tick cases. |
| 31 | PASS | `tests/game/depot/prices.test.ts`: generated 49 operator cell tests, four tier factors at legs 1/12, independent pair rounding. |
| 32 | PASS | `prices.test.ts`: 30-point critical-entry repair limit through status changes/save restore, daemon ceil-half block cost, affliction/clock preservation. |
| 33 | PASS | `prices.test.ts` proves death prerequisite and once-per-run guard across depot visits. `legRunner.test.ts`: `a recruit carries the 60 percent passive, one fewer charge, and binds in the next leg` asserts .6 runner passive, reduced charges, null pid then next-leg rebind, reused identity, full integrity and one retained original tombstone. |
| 34 | PASS | `prices.test.ts`: all four tiers, novice-only remedy text, kernel-space unavailable, live-view numerical snapshot case. |
| 35 | PASS | `legRunner.test.ts`: exact next-leg rollback and one correctness mark; `forfeits the dividend after a protected retry and consumes protection once` asserts retry dividend 0; `expires protection after a successful next leg so a later failure cannot restore it` proves expiry; `a later depot checkpoint restores the later boundary rather than the previous protected leg` proves the new boundary, resources, convoy and RNG replace the old target. Persisted continuation also preserves consumption. |
| 36 | PASS | `tests/game/reclamation/verge.test.ts`: all seven exact parameters at .1/.5/.9, including 1.95; equal seeds and restore equality. |
| 37 | PASS in R10 amended scope; approved deferral | `reclamation/scoring.test.ts`: four explicit scoring fixtures assert 90/150/220/265 quota and matching block/cycle rows. Generated-layout target yields and the .9/.1 identical-play ratio remain explicitly deferred by R10; they are not passed original criteria. The fixtures are tally inputs, not claims of generated playable traces. |
| 38 | PASS | `reclamation/scoring.test.ts`: scoring subtracts 25 quota/live block, lifecycle inflicts 6 integrity and sets four-second blank, and both prove clean-sweep forfeiture. |
| 39 | PASS | `reclamation/scoring.test.ts`: multipliers 1/.6/.36; first free at zero bandwidth; exactly 8 on rounds 2/3; fourth refused; insufficient paid round preserves RNG. R11 test verifies multiplier before block rounding and refund from final quota. |
| 40 | PASS | `tests/game/legSandbox.test.ts`: kernelConfig/populate/createStage/update/evaluate/interaction predicate failures, exact fallbacks, first-update disable, harmless anchor/dispose, healthy pass-through. Runner tests establish continuation after entry/evaluate failures. |
| 41 | PASS | `legRunner.test.ts`: parameterized throwing kernelConfig/populate preserves resources, convoy and streams and allows the next leg. |
| 42 | PASS | `legRunner.test.ts`: `runs a 200-tick synthetic leg headlessly in under 200 ms` runs 100 conservative travel ticks plus five 20-tick real crossing attempts then exits; separate stage-null case and AST boundary scan enforce renderer/DOM/value-import separation. |
| 43 | PASS | `legRunner.test.ts`: `two live runners produce identical outcomes, run states and canonical event hashes` compares all three. Three seeds compare live/registered direct replay; crossing and completed-tick resource actions compare hash/score with zero skipped records. The four terminal-cost cases additionally compare actual pre-evaluate ledgers, hashes and scores on Boot/Fork for architect/kernel_space; the two provisional-continuation cases compare run, kernel, streams and Director state after a refused crossing and another write. |
| 44 | PASS | `run-mutation.test.ts`: read-proxy control, full Director case and actual `LegRunner` case. The latter includes enter, depot/reclamation transactions, event/affliction travel, derezz and actual exit, asserting zero attempted writes through get(), zero diagnostics and both boundary saves. These cases are included in the latest test evidence. |
| 45 | PASS | Recorded `git diff --exit-code -- src/kernel src/game/types.ts src/design/tokens.ts src/render/camera/focusContract.ts` exited 0, a stronger path check than the two required files. The full integration `npm run check:contracts` exited 0; no approved integration edits those protected paths. |
| 46 | PASS | `tests/game/boundaries.test.ts`: AST scan of game code, excluding workers as V21 specifies; detects dotted/computed forbidden calls and global aliases, ignoring comments/strings/types. Existing worker boundary suite remains authoritative for workers. |
| 47 | PASS | Unchanged `tests/kernel/determinism.test.ts`: D1 5000-tick byte equality, D3 stream independence and D4 64-seed sweep all passed in the full 2,156-test integration run, without failures, timeouts or skips. |

## V1 to V23

| Ruling | Coverage | Specific evidence or remaining gap |
|---|---|---|
| V1 | PASS, source scope | Owned barrel/runner/director/sandbox files exist; host/director tests are consolidated in `tests/game/runHost.test.ts` and Runner regressions in `legRunner.test.ts`. No temporary extra suites or loop/main edits remain. `WP-19-file-inventory.md` verifies all 52 full-tree paths, 45 created and 7 modified; the six explicitly approved external integrations are applied. Per-commit SHAs are tracked under V23. |
| V2 | PASS; boot handoff remains a report duty | `runHost.test.ts`: exact command/pre/step/post order, leg-local ticks, complete event capture, stores flushed once per frame, route after flush, live policy telemetry, replacement-kernel subscription, no modal advancement. Optional visual-hook forwarding and full HudLeg contents are not individually asserted. |
| V3 | PASS, amended R4 | `paceRations.test.ts`: active scheduler preserved, quantum/degree binding, entry quantum no-op. `runHost.test.ts`: command batch admission. `legRunner.test.ts`: explicit quantum7 survives pace change through real hooks. A spy asserting exactly one binding per entire drained batch would strengthen the amended schedule requirement. |
| V4 | PASS, corrected by section-5 ruling | `paceRations.test.ts` proves separate economic/kernel tables and `setFramePolicy` retaining allocation scheme/scope. `runHost.test.ts` proves forced-starved policy reaches kernel. Report the original unreferenced-table claim as corrected, and per-Program floor as a kernel follow-up. |
| V5 | PASS | `paceRations.test.ts`: exact synthetic convoy/workload arrival arrays at all four paces, including unchanged zero and nonmutation of input. Live/replay identity covers shared populate use. The original synthetic convoy arrivals are deliberately not rewritten. |
| V6 | PASS with an explicit synthetic threshold | `degree.test.ts`: approved global admission, ceiling 8, clamping, delayed admission, deterministic movement groups and frame accounting. `has one throughput peak and recurrent above-threshold faults at crowded degrees` configures a synthetic threshold 100, preserves kernel timing defaults and demonstrates a unique peak at degree 2, followed by lower throughput and repeated measured faults 125 > 100 at crowded degrees 4 to 8. It observes actual above-threshold thrashing events and recurrent fault samples after warmup; no kernel changes or new ruling are involved. |
| V7 | PASS, approved pending-process exception | `afflictions.test.ts` pins all 13 reason mappings and highest-drain/tie/fallback selection. `runHost.test.ts` asserts admitted-process pathology exit and one epitaph, plus pending Vesper death at tick 5: PCB reason `killed_by_parent`, epitaph reason `storage_corruption`, direct state change and kill syscall, no `process.exited`, no later admission and no duplicate. The separate post-arrival case retains the pathology lifecycle event. `legRunner.test.ts` adds tombstone-backed casualty observation after normal completion. |
| V8 | PASS; follow-up is a report duty | `contention.test.ts` checks current wait-age mean, matching resource filter, live quantum cap and six hand-built views. Final report must file unavailable last-sixteen hold history against kernel track. |
| V9 | PASS by behavior and source review | EventDeck exclusive upper bound is explicitly asserted; deterministic deck/verge restore and provisional resume compare stream states. Live populate uses the explicitly approved `streams.leg.fork(legId)` exception. Full-tree source audit finds named stream creation in `replay/types.ts` and only the sanctioned per-leg population forks in live entry, replay, checkpoint and resume. No added production `Math.random`, wall-clock or crypto RNG calls were found. An automated RNG-construction/fork scan remains optional. |
| V10 | PASS by source and explicit options assertion | `LegRunner.ts` uses KernelImpl and enables devBuild/invariants at live entry, checkpoint reconstruction and resume. `legRunner.test.ts`: `saves a kernel-free boundary with the exact stream state at both entry and exit` spies on createKernel and explicitly asserts both options. Synthetic cases use real kernels. |
| V11 | PASS | `convoy.test.ts`: injected source filtering by reason/member/leg, one uniform draw, exact substitution, deterministic result, throws without matching copy. Report exported EpitaphCopySource interface and copy TODO. |
| V12 | PASS for main bus/sink path | CommandBus protected roundtrip includes deadlock; director cases cover R1 refusal and terminal cost. `runHost.test.ts`: real synchronous getpid, kernel replacement getter, five refused scheduler fields/man sched, quantum success and deadlock mapping all passed targeted run. `legRunner.test.ts` now proves throwing interaction handlers roll back draft changes and mark costly. Dedicated missing-target refusal and ability no-extra-record assertions would be optional coverage improvements; ability execution remains an accepted handoff. Terminal-origin serialization and replay/continuation are exercised by the new Runner regressions. |
| V13 | PASS, approved integrations applied | `legRunner.test.ts` compares live/direct-replay hashes at three seeds, crossing replay, completed-tick resource actions, and live/live outcomes/states. Four terminal-cost regressions compare pre-evaluate ledgers and hashes/scores on Boot/Fork; two provisional-continuation cases verify terminal provenance after a refused crossing. The three-leg economic-entry case checks entry before config/populate/tick-zero admission, bandwidth refills 36/60/36, charge resets and credit. Casualty regressions verify game deaths after normal completion and exclude entry tombstones. Static worker factories remain the accepted boot handoff. The boundary-score regression additionally compares full next-leg score/objective/codex state, score-dependent configuration hashes, and cumulative completion scoring without duplication. |
| V14 | PASS fallback; optional success-path expansion | `legRunner.test.ts`: null worker plus timeout/rejection fallback, debrief/codex promise slots null, retained card, 1500ms budget and one or two requests. Existing planner/phrasing tests cover successful functions. The normal-process-completion casualty case proves the Runner sends the tombstone-backed death to the planner and chooses `priority_aging`, without historical tombstone contamination. A successful Runner phrase/codex case and explicit sequential invocation spy would strengthen integration coverage but are not remaining mandatory test-table cases. |
| V15 | PASS, typed outputs and observed notifications | Runner tests inspect unavailable/debrief notifications and entry/exit codex callback order; mutation tests observe depot/reclamation/debrief kinds; director test asserts onDerezz begin then tombstone. Comprehensive payload assertions for every notification would strengthen coverage. The output union is typechecked and browser consumption remains the accepted boot handoff. |
| V16 | PASS, approved R3 scope | `economyCurve.test.ts` pins seven columns; affliction tests pin four-tier drain/fatal factors and probability clamp. `runHost.test.ts` checks policy/terminal admission and no partial-cost mutation. Four Runner direct-replay cases prove architect/kernel_space cycle and bandwidth charges on zero-tick Boot and Fork; two continuation cases preserve provenance. Kernel-space read charges remain the explicit shell-audit handoff. |
| V17 | PASS | Depot once-per-run/death guard plus `legRunner.test.ts` explicit .6 passive, one-fewer charge, null pid then next-leg binding, reused id/name and permanent tombstone. Report the frozen-id interpretation. |
| V18 | PASS | `legSandbox.test.ts` exact neutral outcome/NULL_STAGE behavior; EventDeck test proves injected predicate false-on-throw plus diagnostic without sandbox coupling. |
| V19 | PASS, model and source evidence | Verge and Reclamation trace tests cover deterministic input, use-after-free and malformed trace refusal. Runner reads the live `externalFragmentation` metric and exports the trace type. A test deliberately varying that metric before opening a round would strengthen the source-level evidence; it is not a separate mandatory test-table case. |
| V20 | PASS | Provisional resume compares kernel/run/RNG/director snapshot and the next tick; open depot/reclamation phases restore without extra decisions; consumed checkpoint stays consumed. New checkpoint expiry and later-boundary tests cover lifetime/replacement. `saves a kernel-free boundary with the exact stream state at both entry and exit` asserts kind, kernel:null, run and full stream states at both boundaries. |
| V21 | PASS | Owned `run-mutation.test.ts` uses recursively readonly proxies only on get() while mutate sees actual root. `boundaries.test.ts` parses TS AST and tests its scanner against every forbidden global and import form. Worker exclusions preserve the existing suite's scope. |
| V22 | PASS, report present | `WP-19-tables-and-handoffs.md` supplies source/implementation tables, all tier values, termination mapping and synthetic arrival arrays, with approved numerical disagreements and preserved handoffs. It now records all six approved applied integrations and accurately distinguishes pending versus admitted process-death events. Full integration and all four per-commit gate sequences passed; the branch is pushed. |
| V23 | PASS | Preflight and all six proposals were approved and applied. Fetch confirmed unchanged origin/main. Four isolated commit trees each passed the exact four gates; each commit has the required final attribution lines, and commit 1 quotes the approved existing-file diffs. `origin/wp-19` is pushed at `1707cadb537fceb6bd9f001469fa5f1299b76704` and the worktree is clean. See the final report for all SHAs and counts. |

## Mandatory coverage additions now present

The four previously identified coverage tasks are now represented by concrete assertions in the current owned tests:

| Task | Current case evidence |
|---|---|
| Travel history and draw cadence | `travelLoop.test.ts`: `keeps every non-Boot leg history monotonic and bounded at %s` checks all thirteen travel legs at all four paces, exact 0/1 endpoints, monotonicity, no overshoot and repeated no-op advancement after completion. `draws at the exact 80-segment tick positions at %s` covers all four paces. Two `trailing draw at %s/%s ends on tick %s with remainder %s` cases pin remainder 4 versus 5. The credit case pins simultaneous ordinary and twentieth-tick extra draws. |
| All nine remedy kinds | `afflictions.test.ts`: `%s matches exactly and rejects same-kind and cross-kind near misses` is parameterized over all nine frozen variants and also asserts no input mutation. `accepts surplus numeric remedies but still requires the correct resource` covers degree/spend sufficiency. |
| Degree fault threshold | `degree.test.ts`: `has one throughput peak and recurrent above-threshold faults at crowded degrees` uses explicit synthetic threshold 100, with unique peak degree 2 and repeated actual rate 125 > 100 and thrashing events at degrees 4 to 8. Kernel timing defaults and production code are unchanged. |
| Checkpoint and boundary persistence | `legRunner.test.ts`: explicit retry dividend 0, consumption once, expiry after successful next leg, later-depot checkpoint restoring its new boundary, and exact entry/exit persist payloads with kernel:null, run and stream states. The createKernel spy checks devBuild and invariant options. |

Other previously reported gaps remain closed: 100,000 resource-floor operations, 10,000 status operations, 100,000 EventDeck draws, all six fatal clocks during prepaid waits, terminal adapter results/refusals, actual Runner mutation guarding, recruit passive/charges/rebinding, the 200-tick speed bound, live/live full outcome/state/hash equality, observable entry/exit callback order, dividend ordering/credit reduction, ordered deduplication and timeout/rejection fallback. The exact duplicate scratch recruit case was removed; the distinct retained recruit cases provide the evidence.

No additional mandatory test-table gap is identified after the six approved changes and their regressions. R10's generated-layout balance remains an approved deferral. The latter includes generated layout/trace balance; the current every-leaked-region live-block assertion must not be described as proof of a stronger largest-leak density or identical-play yield claim. Optional coverage improvements remain explicitly qualified in V2, V3, V9, V12, V14, V15 and V19; they are not new approval requirements or claims that the existing implementation is wrong.

## Six approved integrations and regression evidence

All six exact proposals were explicitly approved and applied. The filenames retain “proposal” as historical artifact names; they do not indicate pending authorization.

| Approved change | Current behavior and regression evidence |
|---|---|
| [R6 protected replay helper](/Users/kylegothman/Documents/Codex/2026-09-15/files-mentioned-by-the-user-kernel/outputs/WP-19-R6-protected-helper-proposal.patch) | Canonical event capture subscribes with `kernel.events.onAny` and unsubscribes, including command-time events. The protected replay comparisons and owned direct-replay cases exercise the helper. The protected and owned cases passed in the full integration suite. |
| [R4 protected degree expectation](/Users/kylegothman/Documents/Codex/2026-09-15/files-mentioned-by-the-user-kernel/outputs/WP-19-R4-degree-test-proposal.patch) | The fixture now expects `[3]` for the approved override-before-populate order, retaining its final-degree assertion. This is independent of V6's measured throughput/fault-threshold evidence. |
| [Terminal provenance](/Users/kylegothman/Documents/Codex/2026-09-15/files-mentioned-by-the-user-kernel/outputs/WP-19-terminal-provenance-proposal.patch) | Existing choice strings carry `[terminal] `; parsing and replay/resume recover origin without frozen fields or extra records. `preserves %s terminal costs (%s cycles, %s bandwidth) through direct replay of %s` covers four architect/kernel_space × Boot/Fork combinations. `preserves %s terminal provenance through provisional continuation` covers both tiers, a low-bandwidth refused crossing and the next write. |
| [Replay economic entry hook](/Users/kylegothman/Documents/Codex/2026-09-15/files-mentioned-by-the-user-kernel/outputs/WP-19-replay-entry-hook-proposal.patch) | `ReplayHooks.enter` runs before config, populate and tick-zero command admission. `runs economic entry before config, populate and tick-zero admission for Boot and successive legs` checks Boot/Fork/Weave order, 36/60/36 bandwidth, reset charges `[2,2,3,2,2]`, credit and 209 ticks. Direct Boot cost cases cover admission-cost flushing when no step occurs. |
| [Pending-process derezz exception](/Users/kylegothman/Documents/Codex/2026-09-15/files-mentioned-by-the-user-kernel/outputs/WP-19-pending-process-death-proposal.patch) | The pending PCB is discarded through init's existing kill syscall. The PCB reason is `killed_by_parent`; the game epitaph retains the pathology. No `process.exited` is emitted on this path: `process.state_changed` and `syscall.invoked` are asserted. A separate post-arrival regression proves the admitted lifecycle exit still carries the pathology. No kernel change is involved. |
| [Casualty observation reconciliation](/Users/kylegothman/Documents/Codex/2026-09-15/files-mentioned-by-the-user-kernel/outputs/WP-19-casualty-observation-proposal.patch) | `observeLeg` accepts optional tombstones and retains legacy event-only behavior. One-to-one member occurrence matching keeps canonical game reason/tick, stable order and multiplicity. Four Runner cases cover legacy behavior, exit N/epitaph N+1 deduplication, repeated deaths/order, and a game death after terminal normal exit through direct replay and live planning. Entry tombstones are sliced out. |

R10's generated-layout target yields and identical-play fragmentation ratio remain an **approved deferral**, with its exact TODO preserved. Accepted ability, epitaph-copy, frame-floor, workload-only admission, shell read-cost, worker factory and browser-assembly handoffs remain in [tables and handoffs](/Users/kylegothman/Documents/Codex/2026-09-15/files-mentioned-by-the-user-kernel/outputs/WP-19-tables-and-handoffs.md). Arbitrary authored outcome casualties without an event or epitaph have no reason/tick to reconstruct; the approved observation change covers Director/tombstone-backed deaths.

## Validation ledger

Confirmed full integration sequence: Node22, temporary environment `VITEST_MAX_WORKERS=2`, no repository configuration edits. The exact commands `npm run check:contracts`, `npm run typecheck`, `npm run test`, and `npm run build` each exited **0**. The test run passed **127 files / 2,156 tests**, with **no skips**, in **78.49 seconds**. This includes all six approved integrations, two pending-versus-admitted Director cases, four casualty cases and seven terminal/entry cases. The earlier four protected failures are resolved; historical pre-approval results are not the current result.

The focused Runner suite also passed all 47 cases. Frozen contracts and kernel paths remain unchanged. Fetch completed and baseline HEAD and fetched `origin/main` both resolve to `22f7b1c`; no merge was needed.

All four independently verified commits have the required attribution and are pushed to `origin/wp-19`. Remote tip `1707cadb537fceb6bd9f001469fa5f1299b76704` matches local HEAD; the worktree is clean. The final report records each commit SHA, count and all four exit codes.
